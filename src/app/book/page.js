import Nav from "@/components/Nav";
import Hero from "@/components/Hero";

const Book = () => {
  return (
    <div>
      <Nav />
      <Hero text="BOOK TABLE" />
    </div>
  );
};

export default Book;
